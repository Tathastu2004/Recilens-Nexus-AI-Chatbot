fastapi
uvicorn[standard]
httpx
python-dotenv
pdfplumber         # For PDF processing
pillow             # For image processing
tqdm               # For progress bars (optional but useful)
    # (future: if official client exists, else use httpx requests)
